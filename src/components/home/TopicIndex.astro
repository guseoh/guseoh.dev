---
import type { CategoryTreeGroup } from "../../utils/categories";
import CategoryMenu from "../CategoryMenu.astro";

interface Props {
  categories: CategoryTreeGroup[];
}

const { categories } = Astro.props as Props;
---

<section class="home-section" aria-label="Categories">
  <div class="home-section__head home-section__head--inline">
    <h2 class="home-section__title">주요 학습/프로젝트 카테고리</h2>
    <a class="home-section__link" href="/categories/">전체 카테고리</a>
  </div>

  {categories.length === 0 ? (
    <p class="section-empty">카테고리가 없습니다.</p>
  ) : (
    <CategoryMenu groups={categories} idPrefix="home-category" variant="home" label="Home category menu" />
  )}
</section>

